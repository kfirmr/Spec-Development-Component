import { createStyles } from "../../create-styles";

export const useStyles = () =>
  createStyles({
    charCounter: {
      fontSize: 13,
      color: "#000",
      fontWeight: 600,
      direction: "ltr",
      letterSpacing: "0.15em",
    },
  });